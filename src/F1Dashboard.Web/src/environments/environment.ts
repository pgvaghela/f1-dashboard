// Production environment (used by the default `ng build`).
// Set apiBaseUrl to your deployed backend's URL once it's hosted, then redeploy.
export const environment = {
  production: true,
  apiBaseUrl: 'https://REPLACE_WITH_YOUR_RENDER_BACKEND_URL/api'
};
